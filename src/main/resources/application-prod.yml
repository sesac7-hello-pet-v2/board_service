spring:
  config:
    activate:
      on-profile: prod
  data:
    mongodb:
      uri: ${MONGODB_URL}
  cloud:
    openfeign:
      image-service: ${IMAGE_SERVICE_URL}
      client:
        config:
          default:
            connect-timeout: 5000
            read-timeout: 5000
            logger-level: full
